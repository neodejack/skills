interface:
  display_name: "Create ExecPlan"
  short_description: "Draft repository ExecPlans"
  default_prompt: "Use $create-execplan to create an ExecPlan for <purpose> based on the bundled PLANS.md guidance."
policy:
  allow_implicit_invocation: false
